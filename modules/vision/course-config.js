/* vision 코스의 4단계 학습 대시보드 콘텐츠 정의.
   셸 로직은 ../shared/course-dashboard.js 에 있고, 이 파일은 데이터만 담는다.

   이론 4개 개념과 실습 3단계의 대응.
     개념 ① 픽셀        → 1단계 필터 토글 · 화면 클릭 시 R·G·B
     개념 ② 분류 vs 탐지 → 1단계 COCO-SSD 상자
     개념 ③ 신뢰도·임계값 → 1단계 슬라이더 → 2단계 혼동행렬 → 3단계 정밀도/재현율
     개념 ④ 전이학습     → 2단계 MobileNet 임베딩
   3단계(미션)는 새 개념을 배우는 곳이 아니라, 2단계에서 만든 분류기를
   실무 지표로 심사하는 곳이다 — 개념 ③을 끝까지 밀어붙이는 자리다.
   개념을 늘리거나 줄이면 precheck 의 concept 인덱스도 함께 옮겨야 한다. */

CourseDashboard.init({
  courseKey: 'vision',
  theory: [
    {
      icon: '<rect x="3" y="3" width="18" height="18" rx="2"/><path d="M9 3v18M15 3v18M3 9h18M3 15h18"/>',
      title: '컴퓨터가 보는 이미지 — 픽셀',
      summary: '컴퓨터에게 사진은 그림이 아니라 숫자 표입니다. 점 하나마다 빨강·초록·파랑 밝기가 0~255로 적혀 있고, 필터도 AI도 결국 이 숫자를 계산하는 일입니다.',
      point: '이미지 = 가로 × 세로 × 3 숫자 격자',
      definition: '<p><b>픽셀(pixel)</b>은 화면을 이루는 점 하나입니다. 픽셀마다 <b>빨강(R)·초록(G)·파랑(B)</b> 밝기가 각각 0~255 사이의 숫자로 적혀 있습니다. 640×480 사진 한 장이면 640 × 480 × 3 = <b>921,600개의 숫자</b>가 되는 셈입니다.</p><p>그래서 "영상을 처리한다"는 말은 결국 <b>이 숫자 표를 계산한다</b>는 뜻입니다. 우리가 아는 효과들도 전부 산수입니다.</p><p>· <b>흑백</b> — 세 숫자를 평균 내 하나로 합칩니다<br>· <b>색 반전</b> — 모든 값을 <b>255 − 값</b> 으로 바꿉니다<br>· <b>윤곽선(엣지)</b> — 옆 픽셀과 숫자 차이가 크면 밝게 칠합니다</p><p>이 셋 중 어느 것도 AI 가 아닙니다. 모델을 불러올 필요도, 학습할 필요도 없습니다.</p>',
      example: '<p>실습 1단계에서 방 조명을 어둡게 해 보세요. 사람 눈에는 <b>같은 물건</b>이지만 픽셀에 적힌 숫자는 전부 뚝 떨어집니다. 사람은 물건을 보고 컴퓨터는 숫자를 본다는 차이가 정확히 여기서 갈립니다.</p><p>필터를 하나씩 바꿔 보세요. <b>[흑백]</b>으로 색을 통째로 버려도, <b>[색 반전]</b>으로 색을 뒤집어도, <b>[윤곽선]</b>만 남겨도 <b>AI 는 여전히 물체를 찾아냅니다.</b> 색 정보가 셋 다 망가졌는데도 인식이 살아 있다는 것은, AI 가 보고 있는 것이 색이 아니라 <b>밝기가 급하게 변하는 자리 — 곧 형태</b>라는 뜻입니다.</p>',
      practice: '<p>이 숫자 격자가 <b>모든 비전 모델의 입력</b>입니다. CNN 의 첫 층이 하는 일도 방금 걸어 본 윤곽선 필터와 같은 종류의 계산입니다 — 작은 필터를 이미지 위로 굴리며 곱하고 더하는 것이죠.</p><p>차이는 하나뿐입니다. 윤곽선 필터는 <b>사람이 숫자를 정해 줬고</b>, CNN 은 그 숫자를 <b>데이터에서 스스로 찾아냅니다.</b></p><p>의료 영상의 대비 보정, 위성 사진의 밴드 합성, 사진 앱의 필터가 전부 같은 픽셀 연산입니다. 딥러닝이 등장하기 훨씬 전부터 쓰였고 지금도 전처리 단계에서 그대로 쓰입니다.</p>'
    },
    {
      icon: '<rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/>',
      title: '이미지 분류 vs 객체 탐지',
      summary: '분류는 사진 한 장에 정답 하나를 답하고, 탐지는 사진 안에서 물체를 찾아 각각 상자를 칩니다. 내놓는 답의 모양 자체가 다릅니다.',
      point: '정답 1개 vs 상자 N개 (0개일 수도 있다)',
      definition: '<p><b>이미지 분류(Image Classification)</b> — 사진 한 장을 넣으면 클래스 하나가 나옵니다. "이 사진은 고양이" 처럼 <b>사진 전체</b>에 이름표를 답니다.</p><p><b>객체 탐지(Object Detection)</b> — 사진 한 장을 넣으면 <b>상자 여러 개</b>가 나옵니다. 상자마다 위치(x, y, 너비, 높이) · 클래스 · 신뢰도가 딸려 옵니다.</p><p>결정적인 차이는 <b>개수가 정해져 있지 않다</b>는 점입니다. 사람이 셋이면 상자 3개, 아무것도 없으면 <b>0개</b>입니다. 분류 모델은 빈 벽을 비춰도 무언가를 답하지만, 탐지 모델은 "없음"이라고 답할 수 있습니다.</p><p>실습에서 쓰는 <b>COCO-SSD</b> 는 사람·컵·의자·노트북 같은 <b>80종</b>을 찾도록 학습되어 있습니다. 이 목록 밖의 물건은 아무리 비춰도 잡히지 않습니다.</p>',
      example: '<p>실습 1단계에서 여러 물건을 한꺼번에 비춰 보세요. 상자가 동시에 여러 개 생깁니다.</p><p>이제 <b>80종 목록에 없는 물건</b>(안경, 필통, 지우개)을 비춰 보세요. 상자가 아예 안 생기거나, 생김새가 비슷한 엉뚱한 이름이 붙습니다. 탐지 모델은 <b>배운 목록 밖을 볼 수 없습니다.</b></p><p>마지막으로 카메라를 빈 벽으로 돌려 보세요. 상자 0개 — 분류 모델이라면 절대 할 수 없는 답입니다.</p>',
      practice: '<p>자율주행의 보행자·차량 위치 파악, 무인 상점의 선반 물건 개수 세기, 공장 불량 검사, 스포츠 중계의 선수 추적이 모두 탐지입니다.</p><p>한 단계 더 나아가면 <b>분할(Segmentation)</b> 이 있습니다. 네모 상자가 아니라 <b>픽셀 단위로</b> 물체의 윤곽을 땁니다. 인물 모드의 배경 흐리기, 의료 영상의 장기 분리가 그것입니다.</p><p>분류 → 탐지 → 분할 순으로 모델이 답해야 할 것이 많아지고, 학습에 필요한 <b>라벨링 비용도 그만큼 커집니다.</b> 그래서 실무에서는 "분할까지 정말 필요한가"를 먼저 따집니다.</p>'
    },
    {
      icon: '<line x1="4" y1="21" x2="4" y2="14"/><line x1="4" y1="10" x2="4" y2="3"/><line x1="12" y1="21" x2="12" y2="12"/><line x1="12" y1="8" x2="12" y2="3"/><line x1="20" y1="21" x2="20" y2="16"/><line x1="20" y1="12" x2="20" y2="3"/><line x1="1" y1="14" x2="7" y2="14"/><line x1="9" y1="8" x2="15" y2="8"/><line x1="17" y1="16" x2="23" y2="16"/>',
      title: '신뢰도와 임계값',
      summary: '모델은 상자마다 "몇 % 확신한다"를 함께 내놓습니다. 몇 %부터 믿을지 정하는 것이 임계값인데, 올리면 헛다리는 줄지만 놓치는 것이 늘어납니다.',
      point: '임계값↑ → 오탐↓ 미탐↑ · 공짜 이득은 없다',
      definition: '<p><b>신뢰도(Confidence Score)</b> 는 모델이 그 상자를 그 클래스라고 <b>얼마나 확신하는지</b>를 0~100% 로 나타낸 값입니다.</p><p>모델은 사실 아주 많은 후보 상자를 만들어 냅니다. 그중 신뢰도가 낮은 것을 버리고 남은 것만 우리에게 보여 주는데, 그 <b>기준선이 임계값(Threshold)</b> 입니다.</p><p>여기서 가장 중요한 것은 <b>임계값을 어느 쪽으로 옮겨도 대가가 따라온다</b>는 점입니다.</p><p>· 임계값을 <b>올리면</b> — 확실한 것만 통과합니다. 헛다리(<b>오탐</b>)는 줄지만, 진짜인데 못 잡는 것(<b>미탐</b>)이 늘어납니다.<br>· 임계값을 <b>내리면</b> — 정반대입니다. 놓치는 건 줄지만 엉뚱한 상자가 늘어납니다.</p><p>그래서 임계값에는 <b>하나의 정답이 없습니다.</b> 어느 쪽 실수가 더 치명적인지가 값을 정합니다.</p>',
      example: '<p>실습 1단계의 임계값 슬라이더를 <b>0.9 까지 올려</b> 보세요. 상자가 거의 사라지고 [걸러짐] 숫자가 치솟습니다. 눈앞에 분명히 있는 컵도 안 잡힙니다.</p><p>이번엔 <b>0.2 까지 내려</b> 보세요. 상자가 우수수 생기는데, 벽이나 그림자에까지 엉뚱한 이름이 붙습니다.</p><p><b>두 설정 중 어느 쪽도 "틀렸다"고 말할 수 없다는 것이 핵심입니다.</b> 무엇을 만들고 있느냐에 따라 정답이 달라집니다.</p>',
      practice: '<p>· <b>자율주행 보행자 탐지</b> — 못 보고 지나치면 사고입니다. 헛브레이크를 감수하고 임계값을 <b>낮게</b> 잡습니다.<br>· <b>무인 상점 자동 결제</b> — 없는 물건을 청구하면 안 됩니다. 못 찍힌 건 사람이 확인하면 되므로 임계값을 <b>높게</b> 잡습니다.<br>· <b>암 검진 보조</b> — 놓친 병변이 치명적입니다. 낮게 잡고 의심 사례를 사람이 다시 봅니다.</p><p>이 저울질을 숫자로 펼쳐 보는 표가 <b>혼동행렬(Confusion Matrix)</b> 입니다. 실습 2단계에서 <b>직접 학습시킨 모델</b>로 확인하게 됩니다.</p><p>현장에서는 이 저울질을 두 지표로 나눠 부릅니다. <b>재현율</b>은 실제 중 잡아낸 비율(놓치지 않은 정도), <b>정밀도</b>는 골라낸 것 중 진짜의 비율(헛짚지 않은 정도)입니다. 기준선을 내리면 재현율이 오르고 정밀도가 내려갑니다 — 실습 3단계에서 직접 움직여 보게 됩니다.</p>'
    },
    {
      icon: '<polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/>',
      title: '전이학습으로 내 분류기 만들기',
      summary: 'ImageNet 사진 128만 장으로 이미 학습된 MobileNet 이 사진을 숫자 1024개로 압축해 줍니다. 우리는 그 위에 아주 작은 분류기 하나만 새로 학습시킵니다.',
      point: '무거운 특징 추출은 빌려 쓰고, 가벼운 판단만 새로 배운다',
      definition: '<p><b>전이학습(Transfer Learning)</b> 은 대규모 데이터로 이미 학습된 모델이 익혀 둔 표현을 가져와 새로운 과제에 재사용하는 방법입니다.</p><p>여기서 <b>MobileNet</b> 은 <b>특징 추출기</b> 역할을 합니다. 사진 한 장을 넣으면 픽셀 수십만 개를 <b>숫자 1024개(임베딩)</b> 로 압축해 내놓습니다. 이 1024개 안에 "둥글다 · 털이 있다 · 세로줄이 있다" 같은 성질이 담겨 있습니다.</p><p>우리가 새로 학습시키는 것은 <b>그 1024개를 받아 라벨을 맞히는 작은 층 하나뿐</b>입니다. 새로 배워야 할 값이 수백만 개에서 수천 개로 줄기 때문에 사진 30장, 몇 초로 끝납니다.</p><p>MobileNet 쪽은 <b>전혀 바뀌지 않습니다.</b> 그대로 얼려 둔 채 가져다 씁니다.</p>',
      example: '<p>실습 2단계에서 클래스 두 개를 각 30장씩 모으고 [학습 시작]을 누르면 몇 초 만에 끝납니다.</p><p>이 30장으로 MobileNet 자체를 처음부터 학습시키는 것은 <b>불가능합니다.</b> ImageNet 128만 장을 며칠간 돌려야 나오는 모델입니다. 이미 만들어진 부분을 빌려 쓰기 때문에 가능한 일이라는 점을 기억하세요.</p><p>제스처 코스와 비교해 보면 재미있습니다. 거기서는 <b>손 관절 좌표 42개</b>가 입력이었고 여기서는 <b>임베딩 1024개</b>입니다. 빌려 오는 특징이 다를 뿐 <b>구조는 완전히 같습니다.</b></p>',
      practice: '<p>사전학습 모델을 가져와 마지막 층만 갈아 끼우는 방식은 현대 딥러닝 응용의 <b>표준</b>입니다. 이미지의 ImageNet 사전학습, 텍스트의 BERT·LLM 파인튜닝이 전부 같은 원리입니다.</p><p>다만 만능은 아닙니다. 빌려 온 특징이 내 문제와 너무 동떨어지면 잘 안 됩니다. MobileNet 은 <b>일상 사물</b>로 배웠기 때문에 X선 사진이나 현미경 이미지에서는 효과가 크게 떨어집니다. 그럴 때는 더 아래층까지 함께 다시 학습시킵니다(파인튜닝).</p><p>밑바닥부터 학습하는 경우가 실무에서 오히려 드물다는 점이, 이 개념이 중요한 이유입니다.</p>'
    }
  ],
  /* 사전 진단(PreCheckOX) — 선별용. 참/거짓을 즉답하는 단문 진술 8문항으로
     theory 4개 개념을 2문항씩 균등하게 덮는다. concept 값은 위 theory 배열의
     인덱스로, 진단 결과가 이론 학습 목록의 "스킵 가능 / 취약 개념" 표시로
     연결된다. 점수·등급은 표기하지 않는다.
     문장만 읽어도 답이 보이는 진술은 선별 기능이 없으므로 넣지 않는다. */
  precheck: [
    {
      concept: 0, answer: true,
      statement: '컴퓨터에게 사진은 픽셀마다 빨강·초록·파랑 밝기를 숫자로 적어 둔 표다',
      explain: '맞습니다. 픽셀 하나에 <b>0~255 짜리 숫자 세 개</b>가 들어 있습니다. 640×480 사진이면 92만 개가 넘는 숫자가 됩니다.'
    },
    {
      concept: 0, answer: false,
      statement: '사진을 흑백으로 바꾸려면 그 일을 학습한 AI 모델이 있어야 한다',
      explain: '아닙니다. 흑백 변환은 <b>빨강·초록·파랑 세 숫자의 평균을 내는 산수</b>입니다. 모델도, 학습도 필요 없습니다.'
    },
    {
      concept: 1, answer: true,
      statement: '이미지 분류는 사진 전체에 정답 하나를 답하고, 객체 탐지는 물체마다 위치와 이름을 찾는다',
      explain: '맞습니다. 탐지는 <b>어디에(위치)</b> 와 <b>무엇이(분류)</b> 를 동시에 답합니다.'
    },
    {
      concept: 1, answer: false,
      statement: '객체 탐지 모델은 화면에 아무것도 없어도 상자를 최소 하나는 그려 낸다',
      explain: '아닙니다. 탐지 모델이 내놓는 상자는 <b>0개일 수도</b> 있습니다. "없음"을 답할 수 있다는 점이 분류 모델과 결정적으로 다릅니다.'
    },
    {
      concept: 2, answer: true,
      statement: '화면에 "Cup 85%" 라고 떴다면 85%는 컵이 차지한 넓이가 아니라 AI 의 확신 정도다',
      explain: '맞습니다. 모델이 보여 주는 % 는 예측한 클래스에 대한 <b>신뢰도(Confidence Score)</b> 입니다.'
    },
    {
      concept: 2, answer: false,
      statement: '신뢰도 임계값을 높이면 잘못된 상자도 줄고 놓치는 물체도 함께 줄어든다',
      explain: '아닙니다. 여기에 <b>공짜 이득은 없습니다.</b> 임계값을 올리면 잘못된 상자(오탐)는 줄지만, 진짜인데 못 잡는 것(<b>미탐</b>)은 오히려 늘어납니다. 그래서 어느 쪽 실수가 더 치명적인지를 보고 값을 정합니다.'
    },
    {
      concept: 3, answer: false,
      statement: '내 사진을 구분하는 모델을 만들려면 수십만 장을 처음부터 다시 학습시켜야 한다',
      explain: '아닙니다. 이미 학습된 MobileNet 이 사진을 <b>숫자 1024개</b>로 압축해 주고, 우리는 그 위에 작은 층 하나만 얹습니다(<b>전이학습</b>). 30장이면 충분합니다.'
    },
    {
      concept: 3, answer: true,
      statement: '같은 대상이라도 배경·각도·거리를 바꿔가며 모을수록 모델이 더 정확해진다',
      explain: '맞습니다. 데이터는 <b>양</b>만이 아니라 <b>다양성</b>이 중요합니다. 배경을 안 바꾸면 AI 가 물체 대신 배경을 외워 버립니다.'
    }
  ],
  /* 최종 평가(FinalQuizMCQ) — 변별용.
     4지선다를 유지하되, 각 문항의 오답에 정답과 혼동하기 쉬운 유사 개념을
     최소 1개 이상 넣어 이론+실습 후의 실제 이해도를 가른다.
     정답 위치(answerIndex)도 흩어 두어 첫 보기만 찍는 전략을 막는다.
     Q5 는 개념 ①·④ 를 함께 요구하는 종합 문항이다. */
  quiz: [
    {
      question: '실습 1단계에서 흑백·색 반전 필터는 AI 모델을 불러오는 시간 없이 곧바로 걸립니다. 그 이유로 가장 정확한 것은?',
      options: [
        '브라우저 안에 아주 가벼운 필터 전용 AI 모델이 미리 들어 있기 때문',
        '필터가 픽셀의 숫자를 직접 계산하는 산수라서 학습된 모델이 아예 필요 없기 때문',
        '필터 결과를 인터넷 서버가 대신 계산해 즉시 보내 주기 때문',
        '카메라 하드웨어가 흑백 모드를 자체적으로 지원하기 때문'
      ],
      answerIndex: 1,
      explain: '흑백은 <b>세 숫자의 평균</b>, 색 반전은 <b>255 − 값</b> 입니다. 둘 다 그 자리에서 끝나는 산수라 모델도 학습도 필요 없습니다. "가벼운 모델이 들어 있다"는 설명은 <b>모든 영상 처리를 AI 로 착각</b>한 것입니다 — 같은 화면에서 흑백으로 바꿔도 탐지가 계속 도는 것을 보면, 필터와 AI 가 서로 다른 층의 일이라는 게 분명해집니다.'
    },
    {
      question: '다음 중 이미지 분류가 아니라 <b>객체 탐지</b>가 반드시 필요한 상황은?',
      options: [
        '사진첩에 쌓인 사진을 "풍경 / 인물 / 음식" 폴더로 자동 정리하기',
        '엑스레이 사진 한 장이 정상인지 비정상인지 판정하기',
        '무인 상점 선반을 비춰 어떤 물건이 몇 개 남았는지 세기',
        '녹음된 목소리가 누구의 것인지 알아맞히기'
      ],
      answerIndex: 2,
      explain: '개수를 세려면 물체 <b>하나하나를 따로 찾아내야</b> 합니다 — 사진 전체에 이름표 하나를 붙이는 분류로는 불가능합니다. 폴더 정리와 정상/비정상 판정은 사진 한 장에 답 하나면 되므로 <b>분류</b>이고, 목소리는 애초에 영상 문제가 아닙니다.'
    },
    {
      question: '엑스레이에서 폐 결절을 찾는 의료 보조 AI 를 만들 때, 신뢰도 임계값을 <b>일부러 낮게</b> 잡는 경우가 많습니다. 그 이유는?',
      options: [
        '임계값을 낮추면 오탐과 미탐이 동시에 줄어들어 성능이 전반적으로 좋아지기 때문',
        '놓친 병변이 헛짚은 것보다 훨씬 치명적이라, 미탐을 줄이는 대신 오탐이 느는 쪽을 택하기 때문',
        '임계값을 낮추면 모델의 신뢰도 계산이 더 정확해지기 때문',
        '임계값이 낮아야 모델이 더 빠르게 추론하기 때문'
      ],
      answerIndex: 1,
      explain: '임계값 조정에 <b>공짜 이득은 없습니다.</b> 낮추면 미탐은 줄지만 오탐은 반드시 늘어납니다. 의료에서는 헛짚은 것은 의사가 다시 보면 되지만 <b>놓친 병변은 되돌릴 수 없으므로</b>, 오탐이 느는 쪽을 감수합니다. "둘 다 줄어든다"는 보기는 이 트레이드오프 자체를 오해한 것입니다.'
    },
    {
      question: '브라우저에서 사진 30장만으로 나만의 분류 모델이 몇 초 만에 학습되는 핵심 원리는?',
      options: [
        '사진을 흑백으로 바꿔 계산량을 3분의 1로 줄이기 때문',
        '모아 둔 30장을 그대로 저장해 두고 새 사진과 하나씩 비교하기 때문',
        '128만 장으로 미리 학습된 MobileNet 이 사진을 숫자 1024개로 압축해 주고, 그 위에 작은 층만 새로 학습하기 때문',
        '학습 계산을 인터넷 서버로 보내 대신 처리한 뒤 결과만 받아오기 때문'
      ],
      answerIndex: 2,
      explain: '무거운 <b>특징 추출</b>은 이미 학습된 MobileNet 이 맡고, 우리는 그 결과(숫자 1024개)를 받아 라벨을 맞히는 <b>작은 층 하나만</b> 새로 배웁니다. "30장을 저장해 두고 비교한다"는 설명은 학습을 <b>암기</b>로 오해한 것입니다 — 모델은 사진을 저장하지 않습니다.'
    },
    {
      question: '실습에서 만든 분류기가 교실에서는 잘 맞았는데, 집에서 켜 보니 거의 다 틀립니다. 원인과 해결책으로 가장 타당한 것은?',
      options: [
        '모델이 물체가 아니라 교실 배경의 픽셀 패턴을 함께 외운 것이다. 집·복도 등 여러 배경에서 데이터를 다시 모아 학습시킨다',
        '집에서는 카메라가 다르게 작동하므로, 교실에서 쓰던 것과 같은 기종의 카메라를 구해 쓴다',
        '학습 반복 횟수가 부족했던 것이다. 교실에서 모은 그 데이터로 반복 횟수만 크게 늘려 다시 학습시킨다',
        '시간이 지나 모델이 낡은 것이다. 최신 버전의 MobileNet 으로 교체한다'
      ],
      answerIndex: 0,
      explain: '모델에게 사진은 <b>숫자 격자 전체</b>입니다 — 물체만 따로 떼어 보지 않습니다. 한 배경에서만 모으면 <b>배경 숫자까지 정답의 일부로 외워 버립니다.</b> 같은 데이터로 반복 횟수만 늘리면 그 배경을 <b>더 확실하게</b> 외울 뿐이고, 모델을 최신으로 바꿔도 내가 준 데이터가 그대로면 결과도 그대로입니다. 비어 있는 상황을 <b>데이터로 채워 주는 것</b>이 유일한 답입니다.'
    }
  ],
  resultCopy: {
    title: '비전 · 영상처리 AI 코스를 완료했어요!',
    body: '객체 탐지로 AI 가 세상을 보는 방식을 관찰하고, 전이학습으로 나만의 분류기를 만든 뒤, 그 분류기를 실제 판정 업무에 투입해 정밀도와 재현율로 도입 여부까지 판단했습니다. 모델을 만드는 일과 현장에 내보내는 일이 서로 다른 문제라는 걸 이해하셨나요?'
  },
  hint: {
    title: '막히면 여기를 확인해요',
    body: '<p><b>초급 · 체험</b>: 카메라로 물건을 비추며 실시간 탐지를 확인하고 필터·임계값을 조절해 보세요.</p><p><b>중급 · 제작</b>: 사진을 30장씩 모아 전이학습을 시키고 혼동행렬을 확인해요.</p><p><b>심화 · 응용</b>: 만든 분류기를 활용해 재현율 90%를 달성하는 미션을 해결해요.</p>'
  }
});
